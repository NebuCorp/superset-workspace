# superset-workspace
Open with vs code
cmd+shift+p
search for "reopen in container"
click on it and wait

localhost:8088 for superset
localhost:5050

postgresdb located at postgres:5432 can use psql in container to log in